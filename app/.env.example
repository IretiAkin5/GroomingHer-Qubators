# Local Postgres (Docker). Do NOT commit real secrets.
DATABASE_URL=postgres://her:herlocal@localhost:5432/groomingher

# Better Auth
BETTER_AUTH_SECRET=change-me-generate-with-openssl-rand-hex-32
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI (Phase 5 wires a real provider; stub until then)
AI_PROVIDER=stub
# AI_API_KEY=

# Files: Cloudflare R2 (prod) / MinIO local dev
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=herlocal
S3_SECRET_KEY=herlocal123
S3_BUCKET=groomingher

# Email
RESEND_API_KEY=
